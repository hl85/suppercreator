# YouTube Transcript: Workflows

Guidelines for advanced segmentation and speaker labeling.

## Chapters (`--chapters`)
Parsed from description timestamps. Groups snippets into readable paragraphs.

## Speaker Identification (`--speakers`)
Requires AI post-processing. 
1. Run with `--speakers`.
2. Spawn sub-agent with `prompts/speaker-transcript.md`.
3. Process raw transcript with metadata context.

(See original SKILL.md for full details ...)
